node_modules/
dist/
.next/
*.tsbuildinfo
.env
.env.local
.DS_Store
coverage/

.vercel
.env*

# The symlink is committed; its target is not. It must be re-included AFTER
# the .env* rule above, or the later pattern wins and the link stops tracking.
!apps/backend/.env
